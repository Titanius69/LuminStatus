[CENTER][SIZE=7][B]LuminStatus[/B][/SIZE]
[SIZE=5]A public status page for your network, generated by the proxy itself.[/SIZE]

[SIZE=4][B]Free · Open source (MIT) · Velocity 3.3+[/B][/SIZE][/CENTER]

[HR][/HR]

[SIZE=6][B]The problem[/B][/SIZE]

When your network goes down, three things happen at the same time:

[LIST]
[*]Your Discord fills up with "is it down?"
[*]Your staff stop fixing the problem because they are busy answering that question
[*]Players assume nobody noticed
[/LIST]

The outage itself is always shorter than the trust it costs. SaaS companies solved this in 2010 with a public status page. Minecraft never got one.

And external monitors like UptimeRobot do not help, because [B]all they can see is whether your proxy port answers[/B]. They cannot see that your survival server is dead while the proxy happily accepts players and dumps them all in the lobby.

The proxy is the only thing on your network that knows the whole picture. That is why this is a Velocity plugin.

[HR][/HR]

[SIZE=6][B]Five minutes to a working page[/B][/SIZE]

[LIST=1]
[*]Drop the jar in [ICODE]plugins/[/ICODE] and start the proxy
[*]Open [ICODE]plugins/luminstatus/public/index.html[/ICODE]
[*]Every server from your [ICODE]velocity.toml[/ICODE] is already on it
[/LIST]

[B]No configuration needed to get a working page.[/B] Servers are discovered automatically. Edit [ICODE]config.conf[/ICODE] later if you want friendly names, groups, or Discord alerts.

[HR][/HR]

[SIZE=6][B]What it actually measures[/B][/SIZE]

[B]Active checks[/B] every 30 seconds — a server list ping to each backend, returning latency, player count and version in one round trip.

[B]Passive checks[/B] for free — connection failures the proxy already sees while routing real players. No extra packets, and it reacts faster than the poll interval.

A server must fail [B]three checks in a row[/B] before the page calls it down. One dropped packet is not an outage.

[SIZE=5][B]Four states, not two[/B][/SIZE]

[LIST]
[*][B]Operational[/B] — reachable, latency in its normal band
[*][B]Degraded[/B] — reachable but 3x slower than its own median
[*][B]Under maintenance[/B] — announced downtime, never alerts
[*][B]Partial / major outage[/B] — some or all of the group is unreachable
[/LIST]

[B]Degraded is the state that earns its keep.[/B] Players complain about lag far more often than they complain about downtime, and a binary up/down page has nothing to say about lag. Degraded is measured against each server's own median latency, not a fixed number, because 90 ms is perfectly normal for a box in another datacentre and alarming for one in the same rack.

[SIZE=5][B]What it does not do[/B][/SIZE]

The proxy does not tick, so it has no TPS. Any proxy status plugin showing you a "proxy TPS" figure is showing you a number it invented. LuminStatus reports availability and latency — what the proxy can genuinely see.

[HR][/HR]

[SIZE=6][B]Incidents, without leaving the game[/B][/SIZE]

During an outage the people who know what is happening are already logged in. Asking them to open a web dashboard first is exactly how status pages end up abandoned. So the whole workflow is a command:

[CODE]/luminstatus incident create major_outage survival-1,survival-2 Database maintenance
/luminstatus incident update 20260721-001 identified Connection pool was exhausted
/luminstatus incident resolve 20260721-001 Pool resized, everything is back[/CODE]

Updates appear on the page instantly, go out to your Discord webhook, and land in the Atom feed. Resolving an incident also clears any maintenance flags it set.

[HR][/HR]

[SIZE=6][B]Publishing: two modes[/B][/SIZE]

[SIZE=5][B]Static export (default, and the one you want)[/B][/SIZE]

Writes [ICODE]index.html[/ICODE], [ICODE]status.json[/ICODE] and [ICODE]feed.atom[/ICODE] to a folder every minute. Point a web server at it, sync it to S3, or push it to GitHub Pages with a two-line cron job.

[B]Why this matters:[/B] a status page hosted on the machine that just went down is not a status page. Most plugins in this space skip this mode entirely, and it is the single most important thing about the whole idea.

The exported HTML also embeds the current data inside it, so it shows real numbers even opened straight off disk with nothing serving it.

[SIZE=5][B]Built-in web server[/B][/SIZE]

One port, serves the page plus [ICODE]/status.json[/ICODE], [ICODE]/feed.atom[/ICODE] and [ICODE]/healthz[/ICODE]. Off by default, since plenty of hosts do not allow a second open port.

[HR][/HR]

[SIZE=6][B]Everything else[/B][/SIZE]

[LIST]
[*][B]90-day uptime history[/B] — a per-day bar for every component, with real uptime percentages
[*][B]Discord webhook alerts[/B] — with a settle timer, so a 20-second blip never reaches your channel
[*][B]Atom feed[/B] — wire the page into anything that reads feeds
[*][B]JSON API[/B] — build your own frontend on top of it
[*][B]Groups[/B] — organise components into Core, Gamemodes, whatever you like
[*][B]Dark mode[/B], responsive down to phones, keyboard accessible, respects reduced-motion
[*][B]Privacy by design[/B] — never emits IPs, host names, ports or stack traces
[/LIST]

[HR][/HR]

[SIZE=6][B]Commands[/B][/SIZE]

All under the permission [ICODE]luminstatus.admin[/ICODE]. Aliases: [ICODE]/lstatus[/ICODE], [ICODE]/netstatus[/ICODE].

[LIST]
[*][ICODE]/luminstatus[/ICODE] — current state of everything, in chat
[*][ICODE]/luminstatus reload[/ICODE] — rereads config, keeps the old one if the new file has a typo
[*][ICODE]/luminstatus refresh[/ICODE] — rebuild and re-export now
[*][ICODE]/luminstatus maintenance <server> <on|off>[/ICODE]
[*][ICODE]/luminstatus incident create|update|resolve|list[/ICODE]
[/LIST]

[HR][/HR]

[SIZE=6][B]Technical notes[/B][/SIZE]

[LIST]
[*][B]Zero shaded dependencies.[/B] Velocity already ships Configurate, Adventure and SLF4J; the JSON handling is about 200 lines in the plugin; the web server is the JDK's own. Nothing to conflict with anything else on your proxy.
[*][B]Tiny jar.[/B] Kilobytes, not megabytes. No React, no build chain, one HTML file with inline CSS and vanilla JS.
[*][B]No web fonts, no CDN.[/B] The page has to load when the rest of your infrastructure is on fire.
[*][B]Atomic writes.[/B] A crash mid-save leaves the previous file intact, not a truncated one.
[*][B]History is counters, not samples.[/B] 90 days for a 50-server network is a few kilobytes. No database.
[/LIST]

[HR][/HR]

[SIZE=6][B]Requirements[/B][/SIZE]

Velocity 3.3.0 or newer, Java 17 or newer. No backend plugin required.

[SIZE=6][B]Source and issues[/B][/SIZE]

MIT licensed. Source, issue tracker and pull requests: [URL]https://github.com/lumin-mc/LuminStatus[/URL]

Bug reports with a log excerpt get fixed. "It doesn't work" does not.
